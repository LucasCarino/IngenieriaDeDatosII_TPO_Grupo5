<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="/stylesheets/style.css" rel="stylesheet">
    <title>Document</title>
</head>
<body class="bg-zinc-100 pt-[70px]">
    <%- include('navbar') %>
    <div class="max-w-2xl mx-auto p-6">
        <h1 class="text-center text-blue-900 text-3xl font-bold mb-6">
            <% if (userName) { %>
                Bienvenido a nuestra tienda en línea, <span class="text-orange-500"><%= userName %></span> 
            <% } else { %>
                Bienvenido a nuestra tienda en línea
            <% } %>
        </h1>
        <p class="text-blue-700 mb-4 text-center">Aquí encontrarás una amplia variedad de productos de alta calidad a precios competitivos. Navega por nuestras categorías y descubre lo que tenemos para ti.</p>
        <a href="/products" class="block w-full text-center mt-6 px-4 py-2 bg-blue-900 text-white rounded hover:bg-blue-700 transition duration-200 cursor-pointer">Ver productos</a>

        <ul class="mt-6 space-y-4">
            <% if(messages.success) { %>
                <li class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4" role="alert">
                    <p class="font-bold">Éxito</p>
                    <p><%= messages.success %></p>
                </li>
            <% } %>
        </ul>
    </div>
</body>
</html>