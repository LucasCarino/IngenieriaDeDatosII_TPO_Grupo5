<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= product.name %></title>
</head>
<body class="bg-teal-200">
    <%- include('navbar') %>
    <div class="max-w-2xl mx-auto p-6">
        <h1 class="text-blue-900 text-2xl font-bold mb-2"><%= product.name %></h1>
        <p class="text-blue-700 mb-2">Precio: $<%= product.price %></p>
        <p class="mb-4"><%= product.description %></p>
        <form action="/cart/add" method="post" class="mb-4">
            <input type="hidden" name="itemId" value="<%= product.id %>">
            <input type="submit" value="Agregar al carrito" class="cursor-pointer px-4 py-2 bg-blue-900 text-white hover:bg-blue-700 transition duration-200">
        </form>
        <a href="/products" class="text-blue-900 hover:text-blue-700 transition duration-200">Regresar al catálogo principal</a> 

        <ul class="space-y-4 mt-4">
            <% if(messages.success) { %>
                <li class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4" role="alert">
                    <p class="font-bold">Éxito</p>
                    <p><%= messages.success %></p>
                </li>
            <% } %>
            <% if(messages.error) { %>
                <li class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4" role="alert">
                    <p class="font-bold">Error</p>
                    <p><%= messages.error %></p>
                </li>
            <% } %>
        </ul>
    </div>
</body>